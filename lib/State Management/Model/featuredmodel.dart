class FeaturedModel{
  int? userId;
  int? Id;
  String image;
  String? title;
  String? about;
  String? price;

  FeaturedModel(this.userId, this.Id, this.image, this.title, this.about, this.price);
}